import styled from "styled-components";

export const Container = styled.button`
  width: 85px;
  height: 36px;
  border-radius: 44px;

  position: absolute;
  top: calc(50% - 30%);
  right: 16px;

  color: var(--pink);

  font-weight: 600;
`;
